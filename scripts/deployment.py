# Script to deploy the chatbot
import subprocess

def deploy_chatbot():
    # Example command to deploy the Flask app (simplified)
    subprocess.run(["flask", "run"])

if __name__ == "__main__":
    deploy_chatbot()
